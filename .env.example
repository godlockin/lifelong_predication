# 命理测算系统环境变量配置

# ===== AI 配置 =====
# Gemini API 密钥 (必填，用于AI润色功能)
GEMINI_API_KEY=your_gemini_api_key_here

# AI 模型选择 (可选，默认: gemini-3.0-flash)
# 可选值: gemini-3.0-flash, gemini-1.5-flash, gemini-1.5-pro
AI_MODEL=gemini-3.0-flash

# AI 请求超时时间（毫秒，可选，默认: 30000）
AI_TIMEOUT=30000

# ===== 服务配置 =====
# 服务端口 (可选，默认: 8787)
PORT=8787

# 默认时区 (可选，默认: 8，即东八区)
DEFAULT_TIMEZONE=8

# 默认经度 (可选，默认: 120，即东八区标准经度)
DEFAULT_LONGITUDE=120

# ===== 跨域配置 =====
# 允许的跨域来源 (可选，默认: *)
CORS_ORIGIN=*

# ===== 管理员配置 =====
# 管理员密码 (必填，用于访问管理员后台)
# 建议使用强密码（至少16个字符）
ADMIN_PASSWORD=your_admin_password_here
